# Cheatsheet

普段使いのキーバインド・設定メモ。

## Neovim - LSP コードジャンプ

LSPがアタッチされたバッファで有効。設定: `config/nvim/init.lua`

### ジャンプ系

| キー | 動作 |
|------|------|
| `gd` | 定義へジャンプ（最頻出） |
| `gD` | 宣言へジャンプ |
| `gr` | 参照一覧（誰が呼んでるか） |
| `gi` | 実装へジャンプ |
| `gt` | 型定義へジャンプ |

### 戻る/進む（Vim標準のジャンプリスト）

| キー | 動作 |
|------|------|
| `Ctrl-o` | 前のカーソル位置へ**戻る** |
| `Ctrl-i` / `Tab` | **進む**（戻った後に元の位置へ） |
| `:jumps` | ジャンプ履歴一覧 |
| `''` | 直前のジャンプ位置へ |
| `Ctrl-^` | 直前に開いていたバッファへ |

### ホバー / 編集系

| キー | 動作 |
|------|------|
| `K` | ホバー（型/ドキュメント表示） |
| `Ctrl-k` | 関数シグネチャ表示 |
| `<Space>rn` | リネーム |
| `<Space>ca` | コードアクション |

### 診断（エラー・警告）

| キー | 動作 |
|------|------|
| `[d` | 前の診断へ |
| `]d` | 次の診断へ |
| `<Space>e` | 診断詳細をフロート表示 |

### LSPサーバー

設定済み: `lua_ls`, `dockerls`, `gopls`, `pyright`

- 状態確認: `:LspInfo` または `:checkhealth lsp`
- pyright導入: `npm install -g pyright`

---

## tmux

prefix = `Ctrl-a`

### ペイン操作

| キー | 動作 |
|------|------|
| `prefix + d` | 右に分割 |
| `prefix + D` | 下に分割 |
| `prefix + w` | ペインを閉じる |
| `prefix + h/j/k/l` | ペイン移動 |
| `prefix + H/J/K/L` | ペインリサイズ（連打可） |

### その他

| キー | 動作 |
|------|------|
| `prefix + r` | 設定再読み込み |
| `prefix + Ctrl-a` | prefix自体を送る |

### 見た目

- 非アクティブペインは背景を `#1c1c1c` に暗くしてフォーカス可視化
- 暗さ調整: `config/tmux/tmux.conf` の `window-style` を変更
  - `#262626` 控えめ / `#1c1c1c` 標準 / `#0a0a0a` 強め
